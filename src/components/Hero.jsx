import './Hero.css'

function Hero() {
  return (
    <section className="hero">
      <h1 className="hero-title">
        Voices that shape the <span className="gradient-text">Future</span>.
      </h1>
      <p className="hero-subtitle">
        A transparent platform for students to share concerns, suggest improvements, and build a better education system together.
      </p>
    </section>
  )
}

export default Hero
